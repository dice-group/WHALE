# Prerequirements

- data should be in .nt (`riot` does the trick)
- declare `owl#Class` and `labels` if you don't have them (`src/declare_class.py` creates a list and then `cat declared.nt >> source.nt`)
